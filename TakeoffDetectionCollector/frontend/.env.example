# Production (Vercel): Railway public URL, no trailing slash.
# Local: leave blank to use the Vite /v1 proxy.
VITE_API_URL=

# Same Supabase project as the backend. Safe to expose (anon key).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
